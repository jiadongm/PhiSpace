% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piScore.R
\name{piScore}
\alias{piScore}
\title{Compute pi-score for ranking differentially expressed genes}
\usage{
piScore(
  de_results,
  logfc_col = "avg_log2FC",
  pval_col = "p_val_adj",
  signed = TRUE,
  pval_floor = .Machine$double.xmin,
  add_column = TRUE,
  rank = FALSE
)
}
\arguments{
\item{de_results}{A data.frame-like object containing differential expression
results.}

\item{logfc_col}{Character. Column containing log2 fold change. If the
default is absent, common alternatives are detected automatically.}

\item{pval_col}{Character. Column containing p-values. If the default is
absent, common alternatives are detected automatically.}

\item{signed}{Logical. If \code{TRUE}, preserve the direction of the log fold
change. If \code{FALSE}, use the unsigned Xiao et al. definition.}

\item{pval_floor}{Numeric. Minimum p-value used before applying
\code{-log10(p)}, avoiding infinite scores when p-values are zero.}

\item{add_column}{Logical. If \code{TRUE}, append \code{pi_score} to \code{de_results}.
If \code{FALSE}, return a numeric vector.}

\item{rank}{Logical. If \code{TRUE} and \code{add_column = TRUE}, append \code{pi_rank}
where rank 1 is the largest pi-score.}
}
\value{
If \code{add_column = TRUE}, a data.frame with \code{pi_score} and optionally
\code{pi_rank} appended. Otherwise, a numeric vector aligned to the input rows.
}
\description{
Combines log fold change and p-value into a single significance score
(Xiao et al. 2014). By default returns the signed variant
\code{log2FC * -log10(p)} for directional ranking; set \code{signed = FALSE}
for the paper's original \code{|log2FC| * -log10(p)}.
}
\references{
Xiao Y, Hsiao T-H, Suresh U, et al. (2014) A novel significance score for
  gene selection and ranking. \emph{Bioinformatics} 30(6):801-807.
  \doi{10.1093/bioinformatics/btr671}
}
