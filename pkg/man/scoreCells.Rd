% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoreCells.R
\name{scoreCells}
\alias{scoreCells}
\title{Score query cells against reference classes}
\usage{
scoreCells(
  reference,
  query,
  class_col = NULL,
  assay_ref = "logcounts",
  assay_query = "logcounts",
  signature_method = c("findMarkers", "mean"),
  marker_test = c("t", "wilcox", "binom"),
  marker_pval_type = "any",
  marker_direction = c("up", "down", "any"),
  n_top_genes = 300,
  n_signature_genes = 100,
  remove_housekeeping = TRUE,
  housekeeping_patterns = c("^Rpl", "^Rps", "^mt-", "^MT-", "^Mrpl", "^Mrps"),
  housekeeping_genes = NULL,
  shared_genes_only = TRUE,
  scoring = c("both", "correlation", "signature"),
  cor_method = c("spearman", "pearson"),
  zscore_signature = TRUE,
  score_prefix = "scoreCells",
  BPPARAM = BiocParallel::SerialParam(),
  verbose = TRUE
)
}
\arguments{
\item{reference}{A \code{SingleCellExperiment} or \code{SpatialExperiment} object.}

\item{query}{A \code{SingleCellExperiment} or \code{SpatialExperiment} object to score.}

\item{class_col}{Character or \code{NULL}. Column in \code{colData(reference)} defining
reference classes. If \code{NULL}, all reference cells are treated as one class.}

\item{assay_ref}{Character. Assay in \code{reference} used for scoring.}

\item{assay_query}{Character. Assay in \code{query} used for scoring.}

\item{signature_method}{Character. Signature selection method. \code{"findMarkers"}
uses \code{scran::findMarkers()} for multi-class references and falls back to
mean-expression ranking for single-class references. \code{"mean"} uses
mean-expression ranking for every class.}

\item{marker_test}{Character. Test type passed to \code{scran::findMarkers()}.}

\item{marker_pval_type}{Character. \code{pval.type} passed to
\code{scran::findMarkers()}.}

\item{marker_direction}{Character. Direction used when selecting markers:
\code{"up"}, \code{"down"}, or \code{"any"}.}

\item{n_top_genes}{Integer. Candidate gene pool per class before removing
housekeeping genes.}

\item{n_signature_genes}{Integer. Number of final signature genes per class.}

\item{remove_housekeeping}{Logical. Whether to remove housekeeping genes from
class signatures.}

\item{housekeeping_patterns}{Character vector of regex patterns identifying
housekeeping genes.}

\item{housekeeping_genes}{Character vector of additional housekeeping genes.
If \code{NULL}, a small built-in list is used.}

\item{shared_genes_only}{Logical. If \code{TRUE}, use the intersection of
reference and query genes. If \code{FALSE}, reference and query row names must be
identical.}

\item{scoring}{Character. Which score matrices to compute: \code{"both"},
\code{"correlation"}, or \code{"signature"}.}

\item{cor_method}{Character. Correlation method: \code{"spearman"} or \code{"pearson"}.}

\item{zscore_signature}{Logical. Whether to z-score signature scores. For
multi-class references, z-scoring is across classes within each cell. For a
single-class reference, z-scoring is across cells.}

\item{score_prefix}{Character. Prefix for output \code{reducedDim()} names.}

\item{BPPARAM}{BiocParallel parameter object passed to
\code{scran::findMarkers()}.}

\item{verbose}{Logical. Whether to emit progress messages.}
}
\value{
The modified \code{query} object with score matrices stored in
\code{reducedDim()} slots and run details stored in \code{metadata(query)$scoreCells}.
}
\description{
\code{scoreCells()} provides a lightweight correlation- and signature-based
fallback for regimes where a full PhiSpace model is not meaningful, such as
single-class references or references with very few classes. It stores score
matrices in \code{reducedDim()} slots with one column per reference class.
}
